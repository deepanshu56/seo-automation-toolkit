# Core HTTP + parsing
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Data handling
pandas>=2.0.0

# Content analysis (TF-IDF, similarity, answer-ability)
scikit-learn>=1.3.0
numpy>=1.24.0

# Google APIs (index_inspector.py)
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0

# Optional: JS rendering for render_gap.py (install separately if needed)
# playwright>=1.40.0
